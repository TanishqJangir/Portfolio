

interface OverlayMenuProps {
    isOpen: boolean;
    onClose: () => void;
}

const OverlayMenu = ({
    isOpen,
    onClose,
}: OverlayMenuProps) => {
    return (
        <>
        </>
    )
};

export default OverlayMenu;